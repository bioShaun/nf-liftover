config {
    testsDir "tests"
    workDir ".nf-test"
    // Legacy-shaped config (no aligner / aligner_envs) so base.config fallbacks are exercised.
    // Tests that need mm2plus must set aligner_envs explicitly in their params block.
    configFile "nextflow.legacy.config"
    profile "test"
}
